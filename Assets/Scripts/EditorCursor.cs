using Drawing;
using UnityEditor;
using UnityEditor.SceneManagement;
using UnityEngine;
using UnityEngine.SceneManagement;

public static class EditorCursor {

    [MenuItem("Tools/Move 3D cursor to mouse position ^G")]
    public static void MoveCursorToMousePosition() {
        // var currentDrawingSceneView = SceneView.currentDrawingSceneView;
        // var camera = currentDrawingSceneView.camera;
        // var mousePosition = Event.current.mousePosition;
        // var mouseRay = camera.ScreenPointToRay(mousePosition);
        //
        // Draw.editor.Cross();
    }
}